sudo: false

language: java

matrix:
  include:
  - jdk: oraclejdk8
    env: TERM=dumb MOCK_MAKER=mock-maker-inline KOTLIN_VERSION=1.2.71
  - jdk: oraclejdk8
    env: TERM=dumb KOTLIN_VERSION=1.2.71


env:
  matrix:
  - TERM=dumb

install:
 - true

script:
  - ./gradlew test

notifications:
  email: false
